# Explicit local adult-operated synthetic demo only. No automatic .env loading.
COMPANION_DEMO_MODE=false
# Generate a new operator secret with secrets.token_urlsafe(32); no default.
COMPANION_DEMO_TOKEN=

